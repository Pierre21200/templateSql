// Plus le template va s'étoffer, plus il serait intéressant de npm install seulements dépendances utiles pour le projet
// Donc en fait de commenter les dépendencies inutiles dans package.json avant de npm install

// cd frontend
// npm install

// cd backend
// npm install
